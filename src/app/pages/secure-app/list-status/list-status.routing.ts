import { Routes } from "@angular/router";
import { ListStatusComponent } from "./list-status.component";

export const ListStatusRoute: Routes = [
    { 
        path: '',
        component: ListStatusComponent,
        title: 'Channel | List Status'
    }
]